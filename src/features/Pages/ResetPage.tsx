import React from "react";
import ResetContent from "../Auth/ResetContent";

const ResetPage: React.FC = () => {
    return (
        <React.Fragment>
            <ResetContent />
        </React.Fragment>
    );
};

export default ResetPage;
