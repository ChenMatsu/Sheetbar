import React from "react";
import Login from "../Auth/LoginContent";

const LoginPage: React.FC = () => {
    return (
        <React.Fragment>
            <Login />
        </React.Fragment>
    );
};

export default LoginPage;
